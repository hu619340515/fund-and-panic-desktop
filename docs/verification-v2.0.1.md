# v2.0.1 验收记录

验收日期：2026-09-11。Windows 11 专业版 10.0.22631 x64。未在独立 Windows 10 机器复测；安装包未签名。

## 缺陷与修复证据

- 原冻结服务入口未调用 `freeze_support()`；真实采集时子进程反复报告缺少 `--database`、`--config` 和 `--log-directory`。修复后通过实际 EXE 的成功、退出码 23、硬超时及 PID 回收测试。
- 实网同轮新浪宽度返回 5,213 只有效股票；东方财富只返回排序后的 100 条，旧实现误认全市场。新版拒绝截断响应并采用完整备用来源；腾讯宽度所用代码表同样检查完整性。
- 新浪 IF 原实现从数值中取前三个大于 100 的字段，误把开盘/高低价当作当前报价。固定映射最新价 3、买一 16、卖一 26，并核验日期时间；离线样本验证最新价与开盘价不同的情况。
- 300 股指 QVIX 当日分时为空，300ETF QVIX 可用。保留备用标的及来源名称，不对不同标的作同指标交叉验证；不将 AKShare 新浪期货封装视为独立上游。

## 自动检查

- `npm.cmd run dist:win` 包含类型检查、40 项桌面测试、应用构建、当前引擎源文件哈希/健康检查与实际冻结 worker 诊断。
- `python -X utf8 -m unittest discover -s engine/tests -v`：67 项中 66 项通过，1 项需显式开启的实网探测跳过。实网验收单独执行，不把跳过项计为通过。
- 开发模式 Electron 验收覆盖真实 30 秒定时器、设置、IPC 校验、关闭驻留/恢复、基金断网错误、引擎重启与模块隔离。

## 实网验收方式

`node packaging/smoke-live.cjs` 启动最终便携版，使用独立中文空用户目录，将 PATH 限于系统目录并清空 Python 环境变量，通过正式 preload/IPC 采集。不使用测试行情、不复制历史数据库。记录来源、数据时间、质量状态、截图及退出检查，输出位于本地 `output/live-201/`，不提交用户数据。

冷启动会显示 `structural_bootstrap`、暂定质量以及缺失特征。近一年正式收盘记录为空时保持空图，成交额历史基线和流动性组件缺项不补造。实网可用性只代表验收时刻，不保证外部接口持续可用。

最终产物串行实网验收通过：北京时间 10:46:24 的真实记录，返回时在两分钟内，指数约 56.80、质量暂定；来源包含腾讯指数、新浪宽度、新浪 IF、东方财富涨跌停及 300ETF QVIX。引擎 PID 保持不变、无参数解析/断管报错、退出后无残留。此前并行安装测试导致一次浏览器上下文关闭，该次不算通过，改为串行后验收成功。

最终内置引擎图表回归通过：当日仅显示 3 条对应日期测试记录，排除 1 条异日记录；近一年仅显示已有 3 条离散收盘记录，无补日期/数值。两个界面导出按钮均通过 IPC 生成 PNG。图表夹具与上述实网结果完全分开。

首次在虚拟环境解释器下执行旧进程测试时出现 launcher PID 与实际 Python PID 不同的断言失败；使用项目规定的 `python` 验收命令后完整通过。该次失败没有作为通过记录。

## 安装与产物

`pwsh -NoProfile -File packaging/smoke-install.ps1` 已通过：真实安装目录 EXE 和便携 EXE 均完成设置、IPC、关闭驻留/恢复、断网基金错误、引擎一次恢复及二次失败隔离、退出清理；静默卸载后用户数据标记仍存在。安装/卸载和其他客户端测试应串行执行，避免安装流程关闭正在验收的客户端。

引擎来源 SHA256：`44fb9f3b5d9290feac22f1e7cb55f8f3f6d46cd95e1bc18bd809f67631e7d125`。

| 文件 | 字节数 | SHA256 |
|---|---:|---|
| FundAndPanic-Setup-2.0.1-x64.exe | 157918194 | `86a562d50a08f081b673284d0b120f12be6fc534ef9021fba02db48064dec6c9` |
| FundAndPanic-Portable-2.0.1-x64.exe | 157690636 | `21139f368e2194e103dede10a32b24526c8650451f8d7ae555d2ee506c4d31a4` |
